import { Typography } from "antd";

const TitleDomain = () => {
  return (
    <Typography.Title
      level={1}
      style={{ textAlign: "left", fontWeight: "300" }}
    >
      Domains
    </Typography.Title>
  );
};

export default TitleDomain;
